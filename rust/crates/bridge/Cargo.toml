[package]
publish = false
edition = "2021"
name = "nix-rust-bridge"
license = "(Apache-2.0 WITH LLVM-exception) OR LGPL-2.1-or-later"
authors = ["silvanshade <silvanshade@users.noreply.github.com>"]
links = "nix-rust-bridge"
# DO NOT EDIT `version`: value replaced at build time in nix derivation
version = "0.0.0"
# DO NOT EDIT `description`: value replaced at build time in nix derivation
description = ""

[lib]
crate-type = [ "staticlib" ] # TODO: support dynamic linking

[build-dependencies]
cxx-build = { version = "1.0", features = ["parallel"] }

[dependencies]
cxx.workspace = true
# pin-project-lite.workspace = true
blake3 = { version = "1.5", features = ["mmap", "rayon"] }
